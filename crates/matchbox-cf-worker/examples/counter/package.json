{
    "name": "matchbox-counter",
    "scripts": {
        "build": "cd ../../../.. && bash crates/matchbox-cf-worker/examples/build.sh crates/matchbox-cf-worker/examples/counter crates/matchbox-cf-worker/examples/counter/CounterListener.bx CounterListener",
        "dev": "npm run build && wrangler dev",
        "deploy": "npm run build && wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
