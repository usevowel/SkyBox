{
    "name": "skybox-todo",
    "scripts": {
        "build": "cd ../../../.. && bash crates/matchbox-cf-worker/examples/build.sh crates/matchbox-cf-worker/examples/todo crates/matchbox-cf-worker/examples/todo/sources/TodoListener.bx TodoApp",
        "dev": "npm run build && npx wrangler dev",
        "deploy": "npm run build && npx wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
