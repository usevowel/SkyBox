@BoxBIF
class {

	/**
	 * @todo Stub — TTS not available on WASM.
	 */
	function invoke(
		required string text,
		struct params = {},
		struct options = {}
	) {
		throw(
			type: "UnsupportedInMatchBox",
			message: "aiSpeak() is not available on MatchBox/WASM."
		);
	}

}
