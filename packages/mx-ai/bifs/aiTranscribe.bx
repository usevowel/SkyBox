@BoxBIF
class {

	/**
	 * @todo Stub — STT not available on WASM.
	 */
	function invoke(
		required any audio,
		struct params = {},
		struct options = {}
	) {
		throw(
			type: "UnsupportedInMatchBox",
			message: "aiTranscribe() is not available on MatchBox/WASM."
		);
	}

}
