@BoxBIF
class {

	/**
	 * @todo Stub — global skills not available on WASM.
	 */
	function invoke() {
		throw(
			type: "UnsupportedInMatchBox",
			message: "aiGlobalSkills() is not available on MatchBox/WASM."
		);
	}

}
