@BoxBIF
class {

	/**
	 * @todo Stub — image generation not available on WASM.
	 */
	function invoke(
		required string prompt,
		struct params = {},
		struct options = {}
	) {
		throw(
			type: "UnsupportedInMatchBox",
			message: "aiImage() is not available on MatchBox/WASM."
		);
	}

}
