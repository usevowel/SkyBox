@BoxBIF
class {

	/**
	 * @todo Stub — MCP client not available on WASM.
	 */
	function invoke() {
		throw(
			type: "UnsupportedInMatchBox",
			message: "MCP() is not available on MatchBox/WASM. MCP requires HTTP/stdio transport."
		);
	}

}
