[workspace]
members = [
    "crates/matchbox-cf-worker",
    "crates/cf-worker-builder",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
